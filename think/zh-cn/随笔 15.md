
如果我把每个维度看成一个线段，然后把它们拼接成一个更长的线段，有一个指针一开始指向0点位置，我们可以控制这个指针往线段的另一个方向移动，我们可以控制移动速度，可以让它停下。也可以在任何时候结束本次控制。而且我们可以把指针指向的点输出给外界
如果我们的速度是匀速的，忽然在某个点停下，然后把它输出给外界，那么这个选择就可以看做是一个加权随机
我们也可以通过移动指针来控制手臂的抬起，通过控制移动指针的速度来控制手臂抬起的速度，让指针停下，来让手臂停下。也可以结束这个动作，做其他的事。